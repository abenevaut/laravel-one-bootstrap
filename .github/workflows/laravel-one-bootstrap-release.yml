name: laravel-one-bootstrap-release

on:
  workflow_dispatch:

env:
  php_version: 8.3

jobs:

  publish-pages:
    runs-on: ubuntu-latest
    concurrency:
      group: laravel-one-bootstrap-publish-pages-${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: false
    steps:

      - name: Checkout project
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate static pages
        uses: abenevaut/opensource/laravel-one@master
        with:
          url: https://laravel-one-bootstrap.abenevaut.dev
          php-version: ${{ env.php_version }}

#      - uses: JamesIves/github-pages-deploy-action@v4
#        with:
#          token: ${{ secrets.ABENEVAUT_GITHUB_TOKEN }}
#          branch: gh-pages
#          folder: dist
